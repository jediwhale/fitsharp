namespace fitSharp.Fit.Runner {
    public class HtmlDecorator {
        public static object AddToStart(string newText, string existingText) {
            return newText + existingText;
        }
    }
}
